{
  "name": "agent-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal AI Operating System — deterministic agent orchestration over GLM 5.2",
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.0.0",
    "@fastify/static": "^8.0.3",
    "fastify": "^5.2.1"
  }
}
